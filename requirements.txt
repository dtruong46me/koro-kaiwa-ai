# --- Core ---
pydantic-settings>=2.2.0

# --- Backend ---
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# --- ASR ---
# Local Whisper (chọn một trong hai)
openai-whisper>=20231117          # local model, cần ffmpeg
# google-cloud-speech>=2.26.0     # Google Cloud STT (tuỳ chọn)

# --- LLM (LangChain) ---
langchain-core>=0.2.0
langchain-openai>=0.1.0
langchain-google-genai>=1.0.0
langchain-anthropic>=0.1.0

# --- TTS ---
# OpenAI TTS dùng chung openai SDK bên dưới
# Voicevox: REST API, không cần Python package

# --- NLP ---
pykakasi>=2.2.1
requests>=2.31.0                  # Cho Google Translate API

# --- Session ---
redis>=5.0.0                      # Chỉ cần khi SESSION_BACKEND=redis

# --- Shared ---
openai>=1.30.0                    # OpenAI ASR + TTS + LLM

# --- Dev / Test ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                     # Test FastAPI
